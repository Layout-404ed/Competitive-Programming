// problem: busstops
// time complexity: 
// i/o: stdio
#include "bits/stdc++.h"
using namespace std;
using ll = long long;
template<typename T>
using v = vector<T>;
#define dbg(v) cout << "Line(" << __LINE__ << ") -> " << #v << " = " << (v) << endl;
int main(void)
{
	ios_base::sync_with_stdio(false); cin.tie(NULL);
    
    // input
    int a, b; cin >> a, b;
    v<v<char>> arr(a, v<char>(b));
    for(auto &i: a)
    {
        
    }

    // algo
    ll x1 = 0;
    for(ll i = 0; i < n; ++i)
        


    for(ll i = 0; i < n; ++i)
    {
        x2 += a[i].first;
        x2 += a[i].second;
    }

    // output
 


    return 0;
}
